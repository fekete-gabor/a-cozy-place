import React from "react";
// import { useMainContext } from "../context/main_context";
import styled from "styled-components";
const News = () => {
  // const { news } = useMainContext();

  return (
    <Wrapper>
      <h2>News</h2>
    </Wrapper>
  );
};

const Wrapper = styled.section`
  min-height: 100vh;
`;
export default News;
